#include <stdio.h>

/**
 * Start - Print  capital 'A' to 'Z'
 * @ch: Putchar
 * Return: Output
 */

int main()
{
	int letter;
	for (letter = 'A'; letter <= 'Z'; letter++)
	{
		putchar(letter);
	}

	return (0);
}
